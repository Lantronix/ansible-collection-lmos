## What changed

## Why

## Checks

- [ ] `ansible-test sanity` and `ansible-test units` pass locally (Python 3.11)
- [ ] `ansible-lint` (production profile) passes
- [ ] README.md / CHANGELOG.rst reviewed as they will render on Automation Hub (no build-from-source, test, or lab instructions; no `pip install`)
- [ ] Customer-visible change: `galaxy.yml` version bumped and CHANGELOG.rst entry added
- [ ] No lab addresses, credentials, or personal paths in tracked files
